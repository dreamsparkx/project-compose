# Starting Instructions
```sh
> docker-compose -f docker-compose.yml up --build -d database
OR
> docker-compose up --build database -d
> docker exec -it <container-id> bash
> mongo -u node --authenticationDatabase node-starter
```